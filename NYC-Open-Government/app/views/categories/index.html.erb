<h1>Timeline</h1>
<ul>
<h3>List of types</h3>
<% @categories.each do |category| %>
    <li>
    <%= category.name %>
    <%= link_to 'Show', category_path(category) %>
    <%= link_to 'Edit', edit_category_path(category) %>
    <%= link_to 'Destroy', category, confirm: 'Are you sure?', method: :delete %>
    </li>   
<% end %>
</ul>
<%= link_to 'Create Event', new_category_path %>